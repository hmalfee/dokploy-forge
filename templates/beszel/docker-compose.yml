services:
  beszel-pre:
    image: python:3.11-slim
    restart: "no"
    volumes:
      - beszel_data:/beszel_data
      - ../files/beszel-pre.py:/app/scripts/beszel-pre.py:ro
    environment:
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@admin.com}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin1234}
    command:
      - sh
      - -c
      - |
        pip install -q bcrypt cryptography
        python3 -u /app/scripts/beszel-pre.py

  beszel-hub:
    image: henrygd/beszel:0.18.7
    restart: unless-stopped
    depends_on:
      beszel-pre: { condition: service_completed_successfully }
    volumes:
      - beszel_data:/beszel_data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      USER_EMAIL: ${ADMIN_EMAIL:-admin@admin.com}
      USER_PASSWORD: ${ADMIN_PASSWORD:-admin1234}
      HEARTBEAT_URL: ${HEARTBEAT_URL}
      HEARTBEAT_INTERVAL: ${HEARTBEAT_INTERVAL}
      HEARTBEAT_METHOD: ${HEARTBEAT_METHOD}
    healthcheck:
      test: ["CMD", "/beszel", "health", "--url", "http://127.0.0.1:8090"]
      interval: 10s
      timeout: 3s
      retries: 5

  beszel-agent:
    image: henrygd/beszel-agent:0.18.7
    restart: unless-stopped
    depends_on:
      beszel-hub: { condition: service_healthy }
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - beszel_data:/beszel_data:ro
    environment:
      PORT: 45876
      HUB_URL: http://beszel-hub:8090
      KEY_FILE: /beszel_data/id_ed25519.pub
      TOKEN_FILE: /beszel_data/agent_token

  beszel-post:
    image: python:3.11-slim
    restart: "no"
    depends_on:
      beszel-agent: { condition: service_started }
    volumes:
      - beszel_data:/beszel_data
      - ../files/beszel-post.py:/app/scripts/beszel-post.py:ro
    environment:
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@admin.com}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin1234}
      HUB_URL: http://beszel-hub:8090
      SYSTEM_NAME: ${SYSTEM_NAME:-Local System}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_SENDER_NAME: ${SMTP_SENDER_NAME:-Beszel}
      SMTP_SENDER_ADDRESS: ${SMTP_SENDER_ADDRESS:-}
    command:
      - sh
      - -c
      - |
        python3 -u /app/scripts/beszel-post.py

volumes:
  beszel_data: {}
